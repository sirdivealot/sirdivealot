const path = require('path')

const jumbo = '/img/sipadan.jpg'

module.exports = function(img) {
	return `
	<div class="jumbo">
		<img src="${path.join(process.env.dir, img || jumbo)}"/>
	</div>
`
}