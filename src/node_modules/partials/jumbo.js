const path = require('path')

const jumbo = `
	<div class="jumbo">
		<img src="${path.join(process.env.dir, '/img/sipadan.jpg')}"/>
	</div>
`

module.exports = function(output) {
	output.write(jumbo)
}